import { Screen } from '@/src/components/layout/Screen';
import { AppText } from '@/src/components/ui/AppText';

export default function Profile() {
  return (
    <Screen>
      <AppText variant="heading">Profile Screen</AppText>
    </Screen>
  );
}
